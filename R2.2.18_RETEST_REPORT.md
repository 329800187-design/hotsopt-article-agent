# RC1.3.3-Lite-R2.2.18 重测报告

日期：2026-08-04

基线提交：`18290dacb56aabbed60ad637f28f6ac6a134d5e6`

修复提交：`68fabc24370739a85796c7422add962fa9f0931f`

## R2.2.17 实测失败

用户任务 `1cd7f09cede2`、`30f38f65c002` 均出现正文和图片生成成功，但 `article.json` 的图片列表为空；对应客户 ZIP 中 Word 的媒体数量为 0。用户同时反馈图片生成期间界面严重卡顿，等待后弹窗并退出，且没有文章确认、图片确认、融合预览和最终导出闭环。

## R2.2.18 修复

- 增加文章确认、图片确认、最终图文稿预览、最终稿确认和导出状态机。
- 图片成功落盘后同步 `article.json`，Word/ZIP 使用同一份最终图片列表。
- 导出前检查最终稿预览和确认状态；融合只做排版，不调用模型。
- 后台 Future 未处理异常写入失败状态和错误码，不再静默停留在生成中。
- 自动保存轮询从 1 秒调整为 5 秒；列表默认不解码原图，查看大图时才加载。
- 增加低成本 2 图方案和单图费用估算入口，默认估算价仅在未配置时使用 0.10 元/图。
- 文章编辑或恢复版本会使下游图片、融合和导出确认失效。

## 检查结果

- 定向流程、图片、导出回归：`35 passed`。
- 变更文件定向安全扫描：`13 files scanned, SECURITY_SCAN_PASS`。
- `compileall`：PASS。
- `git diff --check`：PASS。
- 收集测试：`1033 tests collected`；分片执行受 Windows 命令 120 秒上限影响，重型 GUI 分片未全部在单次命令内返回最终统计；已对超时/环境项单独复核。
- R2.2.18 Source ZIP：已生成，包扫描 PASS。
- Windows 安装包：未生成，本机缺少 .NET SDK，不能伪造安装、卸载、重启和安装版复测通过。
- 真实模型调用、真实额度消耗、用户本人重测：本轮未执行，不能用模拟结果替代。

## 当前结论

`READY_FOR_PACKAGE_BUILD=false`

`READY_FOR_USER_RETEST=false`

`CUSTOMER_DELIVERY_ALLOWED=false`

Windows 安装包构建依赖 .NET SDK；补齐构建环境后，应重新生成 R2.2.18 安装包并由用户复测文章确认、图片融合预览、Word/ZIP 导出、卡顿和崩溃闭环。不得沿用 R2.2.17 的安装包、产物或证据。
