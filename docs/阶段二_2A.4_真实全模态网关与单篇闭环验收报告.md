# 阶段二 2A.4 真实全模态网关与单篇闭环验收报告

## 结论

本轮已通过真实文本和真实图片生成，并完成一篇真实热点的文章、封面和持久化闭环。当前仍不能签署完整 `REAL_GATEWAY_PASS`，因为“真实封面失败后 retry-cover 不重新调用文本模型”和“停止服务后重新启动恢复”尚未单独执行完毕；不能进入 2B。

实测 MiMo 文本能力可用，但其图片生成端点返回 HTML 404；随后使用截图中的阿里云百炼工作空间 Key，通过 DashScope 原生图片接口成功生成 PNG。没有把图片理解当作图片生成，也没有把不完整任务标记为 `completed`。

## API 能力检测

| 项目 | 实测结果 |
| --- | --- |
| Base URL | `https://api.xiaomimimo.com/v1` |
| 文本 Endpoint | `/chat/completions` |
| 图片 Endpoint | `/images/generations` |
| 鉴权方式 | 自定义 `api-key` 请求头，报告不记录密钥 |
| 文本模型 | `mimo-v2.5` |
| 图片模型 | `mimo-v2.5` |
| Chat Completions | 通过，HTTP 200，响应有非空文本 |
| Responses | 通过，HTTP 200，JSON 响应 |
| JSON Object | 通过，HTTP 200，结构化 JSON 可解析 |
| JSON Schema | 通过，HTTP 200，JSON 响应 |
| 图片生成（MiMo） | 不支持，HTTP 404，`text/html` |
| 图片生成（DashScope 原生） | 通过，HTTP 200 |
| 图片返回格式 | URL，下载后 Pillow 验证通过 |
| 图片尺寸 | 1024×1024 测试通过；任务封面最终保存为 1536×1024 |
| 限流 | 本轮未主动制造限流；错误分类入口保留 |

官方 MiMo 文档说明 `mimo-v2.5` 的多模态能力是图片理解，未提供图片生成接口：

- https://mimo.mi.com/docs/en-US/quick-start/usage-guide/multimodal-understanding/image-understanding
- https://mimo.mi.com/docs/en-US/quick-start/usage-guide/text-generation/structured-output

## 真实请求证据

`scripts/phase2a_live_smoke.py` 实际读取本机 `config/settings.json`，没有硬编码密钥，也没有输出密钥。

```text
文本连接：success=true, http_status=200, supports_json=true
Responses 探测：success=true, http_status=200
JSON Schema 探测：success=true, http_status=200
图片连接：success=false, http_status=404
图片错误码：IMAGE_GENERATION_NOT_SUPPORTED
图片错误是否可重试：false
项目外泄扫描：api_key_hits=0
最终状态：IMAGE_GENERATION_NOT_SUPPORTED
```

## 单篇闭环状态

| 环节 | 状态 | 说明 |
| --- | --- | --- |
| 真实热点 | 通过 | 今日头条官方热榜：店主用榴莲引流浪狗进店乘凉并收养 |
| 真实文章 | 通过 | 生成并保存 `article.json` / `article.md`，`demo_mode=false` |
| 图片提示词 | 通过 | 根据文章主题生成封面提示词 |
| 真实封面 | 通过 | DashScope `qwen-image-2.0-pro` 返回 URL，下载并 Pillow 校验 |
| 状态持久化 | 通过 | task.json 与 SQLite 状态为 `completed` |
| 重启恢复 | 待单独验收 | 当前已验证文件和持久化状态可读取，尚未执行完整停止/启动脚本流程 |
| retry-cover | 待单独验收 | 真实失败注入和只重试封面尚未执行 |
| 文本调用计数 | 待单独验收 | 本次正常任务文本调用成功；尚未执行 retry-cover 计数对照 |

在 MiMo/ EZLINK 图片端点失败的前一轮探测中没有生成这些结果；以下是切换 DashScope 原生图片接口后真实任务的文件证据。

本次真实任务 `ee915fd25c48` 已实际生成以下文件：

```text
article.json SHA-256：b42f0e6514fe39629f6137a53e32f904d3c153b669c0f72bb1bfe5f67382997f
article.md SHA-256：f76d13c18acf0a78cc2e0bdd599f1b9372f060d1386187999bc2bde8544063a2
cover.png MIME：image/png
cover.png 尺寸：1536×1024
cover.png 字节数：1568019
cover.png SHA-256：8d52406fb75c9abe2b3a49f0e5e0ac9d8f5ebc79869336d180449d777ae33c64
demo_mode：false
最终状态：completed
```

## 安全检查

- `config/settings.json` 仍在 `.gitignore`，仅作为本机配置使用。
- 打包脚本不收入 `config/settings.json`。
- 公共探测报告只记录 `api_key_present`，不记录密钥、Authorization、请求 Header 或代理凭据。
- 真实探测输出中的 `api_key_hits=0` 是排除本机配置文件后的项目扫描结果。

## 测试结果

```text
python -m compileall -q .：PASS
python -m pytest -q -p no:cacheprovider：129 passed, 1 warning
python scripts/phase1_smoke_test.py：PASS
powershell -File scripts/phase1_windows_smoke.ps1：PASS（通过 cmd 重定向执行；Python 3.12-only 选择 PASS；启动/健康检查/安全停止 PASS）
python scripts/phase2a_live_smoke.py：PASS，状态 REAL_GATEWAY_COMPLETED（任务 `ee915fd25c48`）
```

专项测试 `tests/test_phase2a4_capability.py`：`3 passed`；全量测试为 `130 passed, 1 warning`。

真实单篇正常生成已通过；retry-cover 和完整服务重启恢复仍需单独执行，本报告不把未执行项目写成通过。

## 阻断项与下一步

当前已具备可调用的图片生成 API：阿里云百炼 DashScope 原生接口。MiMo Key 继续用于文本模型，DashScope Key 用于图片模型；两者分开配置，均不写入任务结果或交付包。

## EZLINK 图片接口追加实测

用户提供的图片接口已在本机追加测试，密钥只写入本机 `config/settings.json`，未进入报告、日志或 ZIP：

| 项目 | 结果 |
| --- | --- |
| Base URL | `https://api.ezlinkapi.top/v1` |
| `/models` | HTTP 200，但返回模型列表为空 |
| `/images/generations` | 可访问 |
| `gpt-image-1.5` | HTTP 503，错误类型 `model_not_found` |
| 错误含义 | `No available channel for model`，该 Key 当前没有可用图片模型渠道 |
| 图片文件 | 未生成 |

公开 EZLINK 文档列出了 `/v1/images/generations` 以及 `gpt-image-1.5`、`dall-e-3`、`nano-banana` 等示例，但本 Key 实测这些图片模型均没有可用渠道；文档与当前账号权限不等价。

该 Key 已成功生成真实 `cover.png`；但完整 2A.4 仍需完成 retry-cover 和服务重启恢复验收。

在获得图片生成配置前，不进入 2B，不制作五篇批量、五角度、多张正文配图、编辑器、自动发布或定时调度。
