# 第二阶段 2A.5：真实网关最终收口验收报告

## 结论

本轮只收口 2A.4 的真实网关、重试、重启和证据问题，不进入 2B。验收结果：

- `REAL_GATEWAY_PASS`
- `RETRY_COVER_PASS`
- `RESTART_RECOVERY_PASS`
- `SECURITY_SCAN_PASS`
- 自动化测试：`135 passed, 1 warning`
- Windows 启动、停止、Python 3.12-only 选择和旧 PID 安全测试通过

## API 能力

- 文本网关：MiMo `mimo-v2.5`，Chat Completions HTTP 200，结构化 JSON 校验通过。
- Responses 探针实际发送并校验了响应内容；当前响应为空，记录为 `INVALID_RESPONSE`，不冒充支持。该可选探针不影响本轮实际使用的 Chat Completions 闭环。
- JSON Schema 探针实际返回对象且 `ok` 为布尔值 `true`。
- 图片网关：DashScope Native，模型 `qwen-image-2.0-pro`，真实返回 URL，下载后 Pillow 校验通过。
- 图片结果为 PNG；真实封面尺寸、字节数和 SHA-256 见 `evidence/phase2a5-live/cover_metadata.json`，不在报告中写入密钥或完整请求头。

## 单篇真实闭环

真实热点经过真实文本模型生成 3 个正文 sections，再调用真实图片模型生成封面；任务最终为 `completed`，`demo_mode=false`。保存文件为 `article.json`、`article.md` 和 `images/cover.png`。脱敏任务摘要及文件哈希见 `evidence/phase2a5-live/live_smoke_redacted.json`。

## retry-cover

先将图片模型配置改为必然失败的模型，真实任务进入 `partial_success`，`failed_step=generating_cover`，文章文件保留；恢复 DashScope 配置后调用 `retry-cover`，最终 `completed`。

- 重试前文本调用次数：`1`
- 重试后文本调用次数：`1`
- `article.md` 哈希保持不变；`article.json` 仅更新封面元数据，文章正文未重新生成
- SQLite 与 `task.json` 最终状态均为 `completed`

证据：`evidence/phase2a5-live/retry_cover_evidence.json`。

## 重启恢复

实际执行 `stop.bat`，确认项目服务停止；再执行 `start.bat`，健康检查恢复。历史已完成任务重新读取后仍为 `completed`，历史任务列表可读，并成功创建重启后的新任务卡片；桌面快捷方式存在。

证据：`evidence/phase2a5-live/restart_evidence.json`。

## 安全与交付

安全扫描覆盖运行文件、任务 JSON、缓存、SQLite、日志、输出、提示词、快照和 ZIP；`config/settings.json` 仅作为本机允许项不进入产品包。扫描结果为 `SECURITY_SCAN_PASS`，测试夹具单独分类，不直接忽略 `tests` 目录。

产品 ZIP 不包含本机配置、数据库、日志、输出或真实网关证据；审计上传 ZIP 另行包含 `evidence/phase2a5-live/`。文件清单和 SHA-256 以最终 manifest 为准。

## 未进入范围

本轮未开发五篇批量、五角度、多张正文配图、编辑器、Word 排版、自动发布、定时调度或多用户能力；不得据此宣称 2B 已完成。
