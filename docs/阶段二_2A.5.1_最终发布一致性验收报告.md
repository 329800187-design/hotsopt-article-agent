# 第二阶段 2A.5.1：最终发布一致性验收报告

## 收口范围

本轮只修复最终发布一致性、retry-cover 公开模型信息、DashScope 设置预设、专项测试和证据格式；未进入 2B。

## 修复结果

- `STATUS.md` 增加“当前测试结果”权威章节，最终开发目录测试为 `135 passed, 1 warning`。
- retry-cover 每次运行刷新 `model_info`；最终图片模型为 `qwen-image-2.0-pro`，格式为 `dashscope_native`。
- retry-cover 记录两次封面尝试，首次失败模型和最终成功模型均不含 Key/Header。
- retry-cover 文本调用次数为 `1 → 1`，正文内容哈希前后相同，封面元数据单独更新。
- DashScope 预设同步 base URL、endpoint、model、size、auth_type 和 api_format；已保存星号尺寸在页面按 `x` 显示。
- `restart_evidence.json` 已改为无 BOM UTF-8；任务证据仅保留摘要、模型信息、状态和哈希。
- `test_fixture_hits` 已去重；上传包证据路径与 ZIP 内真实路径一致。

## 测试结果

- `python -m compileall -q .`：通过。
- `python -m pytest -q -p no:cacheprovider`：`135 passed, 1 warning`。
- `python scripts/phase1_smoke_test.py`：通过。
- 已有真实证据继续有效：`REAL_GATEWAY_PASS`、`RETRY_COVER_PASS`、`RESTART_RECOVERY_PASS`、`SECURITY_SCAN_PASS`。
- 本轮未重新执行 live smoke 单篇生成，避免重复收费；retry-cover 使用现有真实网关执行一次最新元数据验证。

## 最终 ZIP 解压二次验收

最终 ZIP 解压到新的空目录后执行：

- 依赖检查/安装：通过。
- `python -m compileall -q .`：通过。
- `python -m pytest -q -p no:cacheprovider`：`135 passed, 1 warning`。
- 无 Key smoke：返回 `REAL_GATEWAY_PENDING`，因为解压目录没有本机 `config/settings.json`，未伪造真实网关通过。
- ZIP 路径统一使用 `/`：通过。
- ZIP 内 `.pyc`：`0`。
- ZIP 内 `config/settings.json`：不存在。
- ZIP manifest `sensitive_hits`：`[]`，状态 `PACKAGE_SCAN_PASS`。

本次解压到全新的临时空目录；解压目录全量测试实际输出为 `135 passed`，无 Key smoke 实际返回 `REAL_GATEWAY_PENDING` 且退出码为 `0`。

## 交付物

- `hotspot-article-agent-phase2a5-final2.zip`
- 对应 manifest
- 审计上传包包含 `evidence/phase2a5-live/`
- 2A.5.1 专项测试：`tests/test_phase2a5_final_closure.py`

最终 ZIP SHA-256 以 `hotspot-article-agent-phase2a5-final2-manifest.json` 为准，避免报告内容参与打包后产生自引用哈希。

本轮完成后停止，不能据此宣称 2B 完成。
